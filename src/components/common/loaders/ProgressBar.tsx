import { View, StyleSheet } from 'react-native';

export default function ProgressBar() {
  return <View style={styles.container} />;
}

const styles = StyleSheet.create({
  container: { height: 4, backgroundColor: '#e0e0e0' },
});
