import { View, StyleSheet } from 'react-native';

export default function BottomSheet() {
  return <View style={styles.sheet} />;
}

const styles = StyleSheet.create({
  sheet: { padding: 16, backgroundColor: '#fff' },
});
