/**
 * DESTINATIONS SECTION
 * 
 * Displays popular destinations with stacked cards.
 * Extracted from homepage for better modularity.
 */

import StackedDestinationCards from '@/components/features/home/StackedDestinationCards';

export default function DestinationsSection() {
  return <StackedDestinationCards />;
}
