import SectionViewAll from '@/components/common/SectionViewAll';

export default function ViewAllMustSee() {
  return (
    <SectionViewAll
      sectionSlug="must-see"
      headerTitle="Must See"
      subtitle="World's top attractions"
    />
  );
}
