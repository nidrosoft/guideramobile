import SectionViewAll from '@/components/common/SectionViewAll';

export default function ViewAllEvents() {
  return (
    <SectionViewAll
      sectionSlug="events"
      headerTitle="Events You May Like"
      subtitle="Upcoming events near you"
    />
  );
}
