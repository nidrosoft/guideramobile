/**
 * CREATE POST ROUTE
 * 
 * Route for creating a new post in a group.
 */

import CreatePostScreen from '@/features/community/screens/CreatePostScreen';

export default function CreatePost() {
  return <CreatePostScreen />;
}
