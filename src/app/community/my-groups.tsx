/**
 * MY GROUPS ROUTE
 *
 * Route for viewing all groups the user has joined.
 */

import MyGroupsScreen from '@/features/community/screens/MyGroupsScreen';

export default function MyGroups() {
  return <MyGroupsScreen />;
}
