/**
 * CREATE GROUP ROUTE
 * 
 * Route for creating a new community/group.
 */

import CreateGroupScreen from '@/features/community/screens/CreateGroupScreen';

export default function CreateGroup() {
  return <CreateGroupScreen />;
}
