/**
 * CONNECT TAB
 * 
 * Main entry point for the Connect feature (formerly Community).
 * Routes to the Connect Hub screen.
 */

import { CommunityHubScreen } from '@/features/community';

export default function Community() {
  return <CommunityHubScreen />;
}
