/**
 * ACCOUNT TAB
 * 
 * Main entry point for user account.
 * Routes to the Account feature module.
 */

import AccountScreen from '@/features/account/screens/AccountScreen';

export default function Account() {
  return <AccountScreen />;
}
