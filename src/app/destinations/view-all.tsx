import SectionViewAll from '@/components/common/SectionViewAll';

export default function ViewAllDestinations() {
  return (
    <SectionViewAll
      sectionSlug="destinations"
      headerTitle="Popular Destinations"
      subtitle="Trending places to visit"
    />
  );
}
