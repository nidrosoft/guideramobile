import SectionViewAll from '@/components/common/SectionViewAll';

export default function ViewAllPlaces() {
  return (
    <SectionViewAll
      sectionSlug="places"
      headerTitle="Popular Places"
      subtitle="Most visited attractions"
    />
  );
}
